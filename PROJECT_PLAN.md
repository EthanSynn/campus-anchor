# 《校园锚点》项目计划与 Demo 说明

更新时间：2026-09-12  参赛版本：线上 Demo 冲刺版

## 一句话介绍

校园锚点是一套围绕大学阶段运行的 AI 成长系统：它理解用户是谁、所在学校和当前阶段，把信息差转化为个性化判断与下一步行动，再把真实经历沉淀成成长档案。

## 本次 Demo 要证明什么

不是证明“我们已经做完了一款完整校园 App”，而是证明这条核心闭环成立：

用户画像 → 当前阶段 → 个性化机会 → AI 解释匹配 → 场景模拟 → 用户选择 → AI 复盘 → 用户确认成长记录。

当前视觉进展：Anchor 品牌系统参考板已由用户确认；独立 Logo/App Icon 文件仍等待外部工具导出，Demo 暂不把参考拼版直接作为页面图标。

Demo AI 视觉进展：Male AI Master 与 Female AI Master 已确认并接入大智能体。大智能体默认展示男 AI 作为虚化背景，用户可切换女 AI；聊天文字、状态和快捷操作位于清晰前景层。

当前交互决定：大智能体的 AI 背景覆盖整个右侧主工作区，不只覆盖头像或聊天卡片；顶部标题、状态、快捷入口和聊天区均处于沉浸式背景之上，左侧导航保留。

## 明天提交的 Demo 范围

### 已实现

- 首页：大一新生当前阶段、专属任务、成长路线、机会摘要、最近成长
- 起点画像：输入期待后更新完成进度
- 机会卡：匹配理由、截止时间、准备周期、行动入口
- 大智能体：阶段化欢迎语、快捷意图、聊天输入、可控响应
- 场景模拟：第一次参加校园 AI 社团；三个可选行动
- AI 复盘：不同选择对应不同反馈
- 成长确认：确认写入或拒绝保存，体现隐私与用户控制
- 日记：成长时间线和已确认记录
- 浅色/深色模式
- Anchor 视觉母体的代码化探索形态

### 明确不宣称已完成

- 真实学校资料检索
- 全网机会自动发现与人工审核后台
- 生产级模型调用
- 多用户登录、权限、支付、计费
- 完整 API 开放平台
- 100+ 场景与视觉资产

## 评委演示脚本（约 3 分钟）

1. 从首页开始：指出“当前阶段 · 新生适应期”，解释首页回答的是“我现在在哪里、现在最值得做什么”。
2. 点击机会：展示创新创业计划，强调推荐理由、准备周期和下一步，不只是资讯聚合。
3. 进入大智能体：点击“模拟一个大学场景”。
4. 选择“介绍暑假做过的 Agent”，展示 AI 复盘。
5. 点击“确认记录这次尝试”，说明长期记录必须由用户确认。
6. 打开日记：展示这次设计如何把行动沉淀为成长档案。
7. 最后用一句话收束：AI 不只是回答“大学是什么”，而是在逐渐理解“我的大学是什么”。

## 赛后 48 小时：从 Demo 到可持续产品

### P0：稳定核心闭环

- 抽出用户、目标、阶段、任务、机会、场景、成长事件的数据模型
- 将本地演示响应替换为 Provider 接口，支持 OpenAI-compatible、其他供应商和本地模型
- 增加演示数据版本、错误状态、加载状态、来源和更新时间
- 写一套 3 分钟演示回归清单

### P1：真实校园价值

- 接入一所学校的公开官方资料，做最小校园知识库
- 设计机会审核台：来源、去重、有效期、审核人、状态
- 让机会推荐依据画像、当前目标、截止时间、准备周期和组队条件
- 把 3–5 个场景做成配置数据而不是页面硬编码

### P2：成长飞轮

- 起点画像五问
- 任务完成与真实行动记录
- AI 识别潜在成长事件并请求用户确认
- 时间线、成长卡片和阶段总结
- 用用户行为动态修正当前目标，但不把第一次选择永久化

### P3：开放平台

- Provider 配置：平台默认模型、用户自带 API Key、企业自部署
- 服务端密钥托管、额度、日志脱敏、速率限制和费用统计
- REST/Webhook/API 文档与 SDK
- 多学校、多角色和权限系统

## 技术方向（暂定，不是最终决策）

- 前端：优先选择便于快速迭代和部署的 Web App，后续再决定原生 App
- 后端：API 层 + Provider 抽象 + Router + 数据库 + 检索层
- 内容：机会与场景采用 JSON/数据库配置，通用 UI 读取配置
- AI：query → routing → relevant retrieval → reasoning → action
- 可靠性：真实来源、更新时间、审核状态和“没有依据时明确说明”
- Demo：本地 fallback；正式环境：服务端调用真实模型，前端绝不暴露平台密钥

## API 与模型策略

Demo 阶段可以不接真实 AI API；稳定演示优先于实时生成。赛后接入时，不把某一家模型写死：

- `AIProvider`：统一生成、结构化输出、错误与超时接口
- `Router`：决定问题是否需要个人资料、学校资料、机会、场景或成长记录
- `ModelPolicy`：按任务选择快模型/强模型，设置预算、超时和 fallback
- `UserProvider`：允许用户配置自己的兼容 API，但密钥只能安全存储，不能进入前端代码或日志

## 免费或低成本 API 的判断

“免费 API”通常意味着新用户试用额度、每日限额、开源模型自托管，或第三方平台的短期活动，不应作为长期商业基础。赛后可以评估：

- OpenAI-compatible 供应商的试用/低价模型
- Google、智谱、硅基流动、DeepSeek 等具体地区与账户可用的免费额度或低价方案
- 本地运行的开源模型（代价从云端 token 转为设备显存、延迟和运维）

选择标准：可用地区、账号验证、速率限制、隐私条款、结构化输出、稳定性、总成本。Demo 不依赖其中任何一个。

## Token 预算判断

500 美元对“Codex 开发一个前端 Demo + 反复调试”通常足够，但不能把它当成无限预算。真正的风险不是代码 token，而是：反复大范围重写、同时做太多页面、真实 API 长上下文调用、图片生成和不受控的自动化测试。

本次建议：使用 Codex 完成工程主线；必要时用更强模型做一次架构审查或疑难调试；不并行引入多个开发工具制造上下文分裂。图片/Logo 探索可单独使用图像工具，但明天不应让视觉资产阻塞功能 Demo。

## 开源与传播

### 可以开源

- 产品理念、Master Context、路线图
- Demo 源码与本地运行说明
- 数据模型、场景配置规范、Provider 接口规范
- 脱敏后的演示数据和设计原则
- 决策记录、复盘与公开开发日志

### 需要谨慎处理

- API Key、个人信息、聊天记录、学校内部文件
- 未获许可的学校通知、图片、Logo、第三方机会资料
- 第三方模型输出、商业 API 的使用条款
- 公开仓库中的默认密钥、生产 URL 和服务器日志

### 传播顺序

先 GitHub 发布可复现 Demo，再发布项目故事、产品闭环和真实开发过程，最后在 X/Twitter 等平台同步短帖。公开表达要诚实区分“已实现、演示模拟、计划中”，这会比夸大完成度更容易获得长期信任与合作。

## 长期定位

这是两个普通大一新生的第一次长期产品尝试，不以短期赚钱为第一目标。商业化是可选的可持续路径：云端托管、自部署、团队版、学校合作和用户自带模型 API；开源、学习、真实使用价值和产品成长优先。
